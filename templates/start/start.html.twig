{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <h1>Utilizator</h1>
        </div>
        <div class="row">
            <h2>
                <ul>
                    {% if user.isClient != null %}
                        <li><a href="{{ path('beneficiary_service_list') }}">Cont beneficiar</a></li>
                    {% endif %}
                    {% if user.isServiceProvider != null %}
                        <li><a href="{{ path('provider_menu') }}">Cont prestator de servicii</a></li>
                    {% endif %}
                 </ul>
            </h2>
        </div>
    </div>
{% endblock %}